# python
__pycache__/
*.pyc
backend/.venv/
backend/data/
.pytest_cache/
.ruff_cache/

# node — match any frontend, not just the original one
node_modules/
dist/
.vite/

# local
.env
.DS_Store

# throwaway: bucket snapshots taken before a write test, scratch payloads.
# Never real code.
ignored/

# Claude Code: launch config + the backend dev script are shared; the
# permission file is per-machine.
.claude/settings.local.json
